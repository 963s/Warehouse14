{
  "//": "TEMPLATE — copy to credentials.json (gitignored) and fill in. Used by the",
  "//1": "EAS `preview`/`production` iOS profiles (credentialsSource: local) so BASEL",
  "//2": "signs the .ipa with his OWN Apple distribution cert — nothing is provisioned",
  "//3": "or purchased on this machine. Drop the two files into apps/mobile/ios/certs/:",
  "//4": "  • dist.p12              — exported Apple Distribution cert + private key",
  "//5": "  • profile.mobileprovision — an Ad Hoc or App Store provisioning profile whose",
  "//6": "    App ID matches app.json -> ios.bundleIdentifier (de.warehouse14.mobile).",
  "//7": "NOTE: `expo prebuild --clean` wipes ios/, so place these AFTER any prebuild and",
  "//8": "right before `eas build`. All cert files + credentials.json are gitignored.",
  "ios": {
    "provisioningProfilePath": "ios/certs/profile.mobileprovision",
    "distributionCertificate": {
      "path": "ios/certs/dist.p12",
      "password": "REPLACE_WITH_YOUR_P12_EXPORT_PASSWORD"
    }
  }
}
