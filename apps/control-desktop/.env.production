# Production build config (Vite loads this in `vite build` / `tauri build`).
# Committed on purpose — the public API endpoint the packaged Owner Desktop
# talks to. Local dev overrides this via .env.local.
VITE_API_BASE_URL=https://api.warehouse14.de
