# Dev-only (loaded by Vite in `dev` mode). Keeps the governance app pointed at
# the local api-cloud so the prod fail-safe default in main.tsx never affects dev.
VITE_API_BASE_URL=http://localhost:3001
